# Placeholder. Written once the runtime contract is settled.
# Requirements: redis-server baked in, no network at runtime,
# separate virtualenvs so redis-py is unreachable from the client package.
FROM python:3.12-slim-bookworm
RUN echo "Dockerfile not yet written" && false
